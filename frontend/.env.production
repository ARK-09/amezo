# Demo deploy: mock the API via MSW in production too (see main.tsx /
# .env.example) since the real backend isn't deployed alongside this
# frontend yet. Delete this file once a real backend URL is wired up.
VITE_USE_MSW=true
